Changes to Other Sections
ResumeExperienceSection, ResumeEducationSection, ResumeSkillsSection, ResumeProjectsSection
Spacing: Add mb-6 between collapsible items by adjusting the space-y-6 in the SortableContext wrapper:
jsx

Collapse

Unwrap

Copy
<div className="space-y-6">
  {/* Items */}
</div>
UI: Ensure inputs use bg-gray-800 text-gray-200 for consistency. Update buttons with hover effects:
jsx

Collapse

Unwrap

Copy
<Button
  variant="ghost"
  size="icon"
  onClick={() => handleDelete(exp.id)}
  className="text-blue-400 hover:text-red-400 hover:bg-blue-950/50"
>
  <Trash2 className="h-4 w-4" />
</Button>
ResumePreviewComponent
Updated Layout: Assuming it’s not fully shown, here’s a suggested structure:
jsx

Collapse

Unwrap

Copy
import { Button, Tabs, TabsList, TabsTrigger } from "@/components/ui";
import { Printer, Download, Edit } from "lucide-react";

export default function ResumePreviewComponent({ resume, onTemplateChange, onDownload, onToggleSkillsDisplay, onSmartAdjust, onEdit }) {
  return (
    <div className="flex flex-col space-y-4">
      <div className="flex justify-between items-center p-4 bg-gray-100">
        <h2 className="text-xl font-semibold">{resume.title}</h2>
        <div className="flex gap-2">
          <Button onClick={onDownload}><Download className="h-4 w-4 mr-2" /> Download</Button>
          <Button onClick={() => window.print()}><Printer className="h-4 w-4 mr-2" /> Print</Button>
          <Button onClick={onEdit}><Edit className="h-4 w-4 mr-2" /> Back to Edit</Button>
        </div>
      </div>
      <Tabs defaultValue={resume.template} onValueChange={onTemplateChange}>
        <TabsList className="grid grid-cols-4 p-1 bg-gray-200">
          {["professional", "modern", "minimal", "creative"].map((template) => (
            <TabsTrigger key={template} value={template} className="capitalize">{template}</TabsTrigger>
          ))}
        </TabsList>
      </Tabs>
      <div className="p-4">
        {/* Render resume content based on template */}
        <div className="border border-gray-300 p-6" style={{ width: "595px", minHeight: "841px" }}>
          {/* Placeholder for resume content */}
        </div>
      </div>
    </div>
  );
}
CSS Adjustments
Ensure cosmic-input is defined in your CSS:

css

Collapse

Unwrap

Copy
.cosmic-input {
  background-color: #1f2937; /* bg-gray-800 */
  color: #e5e7eb; /* text-gray-200 */
  border: 1px solid #374151; /* border-gray-700 */
  padding: 0.5rem 1rem;
  border-radius: 0.375rem;
}
.cosmic-input:focus {
  border-color: #3b82f6; /* blue-500 */
  outline: none;
  box-shadow: 0 0 0 1px #3b82f6;
}